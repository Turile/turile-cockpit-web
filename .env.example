# PUBLIC values only. service_role / Shopify / Resend keys must NEVER
# appear here or anywhere in this repo — they live in Supabase secrets.
VITE_SUPABASE_URL=https://bszccqdyzoufnquafztx.supabase.co
# Public anon key, gated by RLS (public read of active experiences only) —
# used by the exchange browse screen for a direct PostgREST read. Safe to
# embed in the frontend bundle by design; get the value from
# `supabase projects api-keys`. Must also be set in Vercel's project env vars.
VITE_SUPABASE_ANON_KEY=
